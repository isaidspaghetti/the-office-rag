# Minimal requirements for Streamlit Cloud + local dashboards
streamlit>=1.31
python-dotenv>=1.0

# Core RAG deps (used by ingestion/evals/scoring)
langchain-openai>=0.3.0
langchain-core>=0.3.0
langchain-chroma>=0.2.0
langchain-community>=0.3.0
chromadb>=0.5.0

# Optional: Qdrant backend for deployed Chat & Debug
qdrant-client>=1.10.0

# Optional: plotting support for experiments/plot_phase_scores.py
matplotlib>=3.8
